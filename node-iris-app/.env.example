# ------------------------------------------------------------
# Node-iris 기본 환경 변수 샘플
#   1) 이 파일을 복사해서 `.env`로 저장하세요.
#   2) 기기/서버 IP에 맞춰 값을 수정하세요.
# ------------------------------------------------------------

# IRIS WebSocket/HTTP 엔드포인트 (루팅 단말 대시보드 URL과 동일)
# 개발 중에는 MOCK_IRIS=true면 연결을 생략합니다.
IRIS_URL=http://127.0.0.1:3000
MOCK_IRIS=true

# 로그/디버깅 설정
# NOTE: (로그 파이프라인 안정성) 기본값은 항상 true.
# 필요 시 테스트 환경에서만 명시적으로 false 로 설정한다.
SAVE_CHAT_LOGS=true
LOG_LEVEL=debug

# 서비스별 데이터 저장 경로
MESSAGE_LOG_DIR=./data/logs
BROADCAST_DB=./data/broadcast-queue.json

# 카카오링크(선택 사항) – 실제 운영 시 필수 값으로 교체
IRIS_HOST=http://127.0.0.1:3000
KAKAOLINK_APP_KEY=
KAKAOLINK_ORIGIN=https://your-service.example.com

# Bootstrap 컨트롤러에서 사용하는 스케줄러 기본값
BATCH_SIZE=100
SCHEDULE_INTERVAL=5000
ENABLE_REMINDERS=true
ENABLE_NOTIFICATIONS=true
